/* hexString.h */
//TODO define MAKRO to only include this once

extern int HexChar2Int(char);

extern int HexNibble2Char(char, char*);

extern int HexByte2Chars(char, char*);

extern int HexString2CharArray(const char*, char*);

